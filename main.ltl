// ltl-pcap-read/main.ltl
//
// libpcap-format capture-file reader with IPv4/TCP dissectors.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-pcap-read v0.1.0")
    io::println("libpcap-format capture-file reader with IPv4/TCP dissectors.")
    0
}
