// examples/demo.ltl — runnable demo for ltl_pcap_read

import std::io
use ltl_pcap_read as lib

fn main() {
    io::println("ltl_pcap_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}